version: "3.0"

services:
    django:
        build: ./Python/
        container_name: django
        volumes:
            - ./..:/usr/src/app
        ports:
            - 8000:8000
        command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]
